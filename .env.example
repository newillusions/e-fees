# E-Fees Development Environment Configuration Template
# Copy this file to .env or .env.dev and fill in your actual values

# Database Settings
# For development: Use your local or development SurrealDB instance
# For production: Update to production database credentials
SURREALDB_URL=ws://YOUR_DATABASE_HOST:8000/rpc
SURREALDB_NS=your_namespace
SURREALDB_DB=your_database
SURREALDB_USER=your_username
SURREALDB_PASS=your_secure_password

# Other settings
BASE_PATH=projects

# Staff Details
# These can also be configured through the application UI
STAFF_NAME=Your Full Name
STAFF_EMAIL=your.email@company.com
STAFF_PHONE=+000 00 000 0000
STAFF_POSITION=Your Position

# Project Templates
# Update these paths to match your local file system
TEMPLATE_BASE_PATH=/path/to/your/base
PROJECT_FOLDERS_PATH=/path/to/your/projects
PROJECT_ARCHIVES_PATH=/path/to/your/archives

# Security Notes:
# - NEVER commit .env, .env.dev, or .env.local files to version control
# - Use strong, unique passwords for database access
# - Keep database credentials secure and rotate them regularly
# - Ensure your SurrealDB instance is properly secured
# - Use environment-specific .env files (.env.dev, .env.prod, etc.)
# - The .gitignore file should exclude all .env* files except .env.example
