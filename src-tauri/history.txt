#V2
SELECT status FROM rfp GROUP BY status;
SELECT stage FROM rfp WHERE stage IS NOT NULL GROUP BY stage;
SELECT status FROM projects GROUP BY status;
INFO FOR TABLE rfp;
INFO FOR TABLE projects;
SELECT name FROM information_schema.tables;
INFO FOR DB;
INFO FOR TABLE fee;
DEFINE FIELD status ON rfp TYPE string DEFAULT 'Draft' ASSERT $value INSIDE ['Draft', 'Prepared', 'Active', 'Sent', 'Under Review', 'Clarification', 'Negotiation', 'Awarded', 'Lost', 'Cancelled', 'Completed', 'On Hold', 'Revised'] PERMISSIONS FULL;
REMOVE FIELD status ON rfp;
DEFINE FIELD status ON rfp TYPE string DEFAULT 'Draft' ASSERT $value INSIDE ['Draft', 'Prepared', 'Active', 'Sent', 'Under Review', 'Clarification', 'Negotiation', 'Awarded', 'Lost', 'Cancelled', 'Completed', 'On Hold', 'Revised'] PERMISSIONS FULL;
REMOVE FIELD status ON fee; DEFINE FIELD status ON fee TYPE string DEFAULT 'Draft' ASSERT $value INSIDE ['Draft', 'Prepared', 'Active', 'Sent', 'Under Review', 'Clarification', 'Negotiation', 'Awarded', 'Lost', 'Cancelled', 'Completed', 'On Hold', 'Revised'] PERMISSIONS FULL;
REMOVE FIELD status ON projects; DEFINE FIELD status ON projects TYPE string ASSERT $value INSIDE ['Draft', 'RFP', 'Active', 'On Hold', 'Completed', 'Cancelled', 'Lost', 'Awarded', 'Revised'] PERMISSIONS FOR select, create, update WHERE FULL;
SELECT status FROM rfp GROUP BY status;
SELECT status FROM projects GROUP BY status;
UPDATE rfp SET status = 'Negotiation' WHERE status = 'Under Review';
UPDATE fee SET status = 'Negotiation' WHERE status = 'Under Review';
REMOVE FIELD status ON rfp; DEFINE FIELD status ON rfp TYPE string DEFAULT 'Draft' ASSERT $value INSIDE ['Draft', 'Sent', 'Negotiation', 'Awarded', 'Completed', 'Lost', 'Cancelled', 'On Hold', 'Revised'] PERMISSIONS FULL;
REMOVE FIELD status ON fee; DEFINE FIELD status ON fee TYPE string DEFAULT 'Draft' ASSERT $value INSIDE ['Draft', 'Sent', 'Negotiation', 'Awarded', 'Completed', 'Lost', 'Cancelled', 'On Hold', 'Revised'] PERMISSIONS FULL;
REMOVE FIELD status ON projects; DEFINE FIELD status ON projects TYPE string ASSERT $value INSIDE ['Draft', 'RFP', 'Active', 'Awarded', 'Completed', 'Lost', 'Cancelled', 'On Hold', 'Revised'] PERMISSIONS FOR select, create, update WHERE FULL;
